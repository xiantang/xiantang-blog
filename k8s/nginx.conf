server {
    listen 80;
    listen [::]:80;

    server_name vim0.com www.vim0.com _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;
}
